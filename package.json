{
  "name": "@f/is-action",
  "description": "Check if standard action.",
  "repository": "git://github.com/micro-js/is-action.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-object": "^1.1.1",
    "@f/is-undefined": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
